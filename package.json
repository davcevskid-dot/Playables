{
  "name": "forward-platformer",
  "version": "1.0.0",
  "description": "FORWARD! - Side-scrolling survival platformer",
  "scripts": {
    "build": "cp index.html www/index.html",
    "sync": "npm run build && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
